import { AbstractHandler } from './AbstractHandler';
import { Candidate } from '../classes/Candidate';

export class DirectoryHandler extends AbstractHandler
{
    Perform(candidate: Candidate, target: number[]): number[]
    {
        return [];
    }

    // to do
}